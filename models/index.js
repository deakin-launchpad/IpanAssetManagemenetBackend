const programModel = require('./program_model');
const moduleModel = require('./module_model')
const activityModel = require('./activities_model')

const models = {
    programModel: programModel,
    moduleModel: moduleModel,
    activityModel: activityModel
}

module.exports = models